import renderSpinner from './renderSpinner'


export const state = { recipe: {} }

export async function loadRecipe (id) {
    try {
        const recipe = {...state}
        let response = null
        let data = null
        if(res){

        }
    } catch (err) {
        console.warn('loadRecipe error', err);
    }
}
